# Test a module global variable and an extern global variable

test_data = {
    "desc": "Module global and extern global",
    "modules": [["mod_globals2.c"]],
    "required": ["Running test 'mod_globals2'"]
}
