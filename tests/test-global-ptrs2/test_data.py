# Same as test-global-ptrs1, but uses various cv qualifiers

test_data = {
    "desc": "Global pointers (const and not const) to data and code",
    "modules": [["mod_global_ptrs2.c"]],
    "required": ["Running test 'mod_global_ptrs2'"]
}
